application:
  development_mode:       false
caching_url:
  meta_table_ttl:         7200000   # time to live (ms)
  meta_constants_ttl:     7200000   # time to live (ms)
  meta_pretty_print_ttl:  7200000   # time to live (ms)
task_monitor:
  check_frequency:        60     # check frequency for task status threads (seconds)
assembly:
  tail_log_frequency:     10     # frequency between requests (seconds)
  debug_task_frequency:    5     # frequency between requests (seconds)